pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "tauri-plugin-solmusic-storage"
include(":tauri-android")
project(":tauri-android").projectDir = new File(".tauri/tauri-api")
include(":solmusic-storage")
project(":solmusic-storage").projectDir = new File(".")
