[package]
name = "solmusic-sqlite"
version = "0.4.2"
edition = "2021"
publish = false

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
solmusic-application = { path = "../solmusic-application" }
solmusic-domain = { path = "../solmusic-domain" }
tracing = "0.1"
uuid = { version = "1", features = ["v7"] }
