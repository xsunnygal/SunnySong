[package]
name = "solmusic-application"
version = "0.3.1"
edition = "2021"
publish = false

[dependencies]
async-trait = "0.1"
futures = "0.3"
solmusic-domain = { path = "../solmusic-domain" }
thiserror = "2"
tracing = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt", "sync"] }
